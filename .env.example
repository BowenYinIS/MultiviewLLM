# Machine-specific data paths configuration
# Copy this file to .env and update paths for your machine

# Raw data directory (where act_info.feather and sample_transaction.feather are stored)
RAW_DATA_DIR=/path/to/your/data/raw_data

# Processed data directory (where generated datasets are stored)
PROCESSED_DATA_DIR=/path/to/your/data/processed_data

# Checkpoint directory (where model checkpoints are stored)
CHECKPOINT_DIR=/path/to/your/checkpoint
